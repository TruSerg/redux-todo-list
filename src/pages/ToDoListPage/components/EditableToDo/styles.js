const styles = () => ({
  wrapper: {
    display: "flex",
    alignItems: "center",
    marginBottom: "10px",
  },

  input: {
    minWidth: "700px",
    minHeight: "50px",
    borderRadius: "5px",
    border: "0",
    backgroundColor: "#DEB887",
    padding: "10px",
  },
});

export default styles;
